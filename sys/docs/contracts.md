# Hợp đồng v3

Quy trình công khai: content → media → video, xem [workflow.md](workflow.md).
Mode review/auto cố định tại lúc tạo job. Mỗi mốc có manifest, revision, snapshot hash các module đầu vào và quyết định riêng actor=user/machine; workflow.approved() còn đối chiếu quyết định với một dòng trong bảng events (SQLite) có hash khớp, cho cả actor user lẫn machine — không phải bảo đảm mật mã, chỉ nâng mức giả mạo từ "ghi một file" lên "ghi file và giả dòng SQLite", cùng mức bảo vệ như workflow.json. Máy kiểm tra kỹ thuật nội bộ không tạo phản hồi người dùng.

Nội bộ giữ control/content/images/audio/render và envelope theo schemas/. Đây là định dạng dữ liệu, không phải năm điểm duyệt. images chỉ có references/final nội bộ; không còn first-three hay ảnh proof riêng. Media gồm ảnh cuối và audio cùng snapshot, không thể duyệt một nửa để mở render. Trên đường sản xuất, `workflow.STAGES['media']` chạy audio trước images (âm thanh local rẻ, đo thời lượng thật trước khi tốn credit ảnh); trong `Pilot.gate` hai module này vẫn độc lập để flow-login/flow-preflight dùng được bất cứ lúc nào.

Đổi content làm mất hiệu lực media/video. Đổi ảnh giữ audio nhưng buộc duyệt lại media/video; vân tay ảnh (identity trong image_pipeline.request()) gắn theo target/prompt/edits của chính ảnh, không theo hash toàn bộ kịch bản, nên sửa một cảnh không làm mất hiệu lực ảnh cảnh khác. content-v3 không còn estimated_seconds trên từng cảnh và không bị validate_content gate theo tổng ước tính (mã ESTIMATE chỉ còn áp dụng content-v2 lịch sử); thời lượng thật do WAV quyết định ở audio. Không sửa revisions. Hash file được kiểm tra lại trước chuyển bước; báo cáo máy phải khớp artifact đã xem.

Việt: Gwen-TTS (`tts_engine=gwen`) nhân bản giọng trong `assets/voices/<tts_voice>/`, đọc từng câu trên GPU, tự kiểm độ dài từng câu và đọc lại câu hỏng; VieNeu vẫn chọn được bằng `tts_engine=vieneu` khi cài lại `.venv-tts`. Anh Pocket TTS CPU INT8 Alba tốc độ gốc (cần `.venv-en`; hiện đã gỡ nên bản có tiếng Anh sẽ dừng ở Blocked). Tiếng Anh có timeline riêng. Phụ đề cắt một lần ở Python (adapters.subtitle_cues); .srt và video dùng chung danh sách cue đó, căn theo đoạn WAV, chưa phải word-level alignment; bản 16:9 ẩn phụ đề nên layout.json ghi applies:false thay vì kiểm một thứ không tồn tại. content-v3 yêu cầu coverage.quote_en trích nguyên văn từ narration_en của đúng cảnh khi bản có tiếng Anh. Structural validation không chứng minh đúng ngữ nghĩa; review mode dùng người dùng, auto dùng báo cáo đánh giá thật và chặn unsupported.
